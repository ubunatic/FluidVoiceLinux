// Phase 0 compile-gating placeholder for the Linux migration (see
// docs/LINUX_MIGRATION_BRANCH_PLAN.md). This only proves the Package.swift manifest
// gates macOS-only targets out of a Linux build. The real "hello swift" canary content
// is Phase 2's job.
print("stub")
